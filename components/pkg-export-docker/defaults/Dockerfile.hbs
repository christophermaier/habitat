FROM scratch
ENV PATH {{path}}
ADD {{rootfs}} /

# TODO (CM): Address this environment variable once and for all
# ENV HAB_NON_ROOT=1

RUN chmod -R g=u /hab
RUN mkdir -p /home/hab_non_root && chmod -R g=u /home/hab_non_root

EXPOSE 9631 {{exposes}}
ENTRYPOINT ["/init.sh"]
CMD ["start", "{{primary_svc_ident}}"]
